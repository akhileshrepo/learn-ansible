- name: INstall Nginx
  hosts: test
  become: true
  tasks:
    - name: Install nginx
      ansible.builtin.yum:
        name: httpd
        state: latest

    - name: Start Nginx
      ansible.builtin.systemd_service:
        name: nginx
        state: started
        enabled: yes

#- name: Install Nginx
#  hosts: WEB
#  become: true
#  roles:
#    - sample