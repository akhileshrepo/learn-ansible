- name: Demo on loops
  hosts: localhost
  connection: local
  vars:
    fruits:
      - apple
      - banana
      - pineapple
      - sweet potato
  tasks:
    - name: Print Fruit Names
      ansible.builtin.debug:
        msg: Fruit Name - {{ item }}
      loops: {{ fruits }}
